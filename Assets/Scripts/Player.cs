using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Player : Cyrcle
{
     
    public override void assignSize(Vector3 currentSize)
    {
        base.assignSize(currentSize);
    }
}
